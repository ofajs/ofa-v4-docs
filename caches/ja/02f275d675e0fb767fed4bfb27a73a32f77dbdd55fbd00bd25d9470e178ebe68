```javascript
// MyComponent.mjs
export default () => {
  return {
    attached(){
      const appTag = this.app;

      const currentPage = appTag.current;
      console.log(`現在アクティブなページは以下の通り：${currentPage}`);
    }
  };
}
```